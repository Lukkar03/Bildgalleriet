{
  "name": "gallery",
  "type": "module",
  "scripts": {
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --coverage"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^30.2.0"
  }
}
